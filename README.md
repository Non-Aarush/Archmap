# Archmap

Archmap analyzes a public Git repository by simply ingesting its repository URL. The system extracts structural signals (modules, services, APIs, DB schemas, infrastructure dependencies) and renders an interactive, explorable architecture map that stays in sync with the code.
